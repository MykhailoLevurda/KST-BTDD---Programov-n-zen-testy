apiVersion: v1
kind: Secret
metadata:
  name: tool-rental-secret
type: Opaque
stringData:
  # Heslo k PostgreSQL – NEDÁVEJ sem reálné heslo v produkci.
  # Pro lokální/staging cluster si ho můžeš vyplnit ručně před aplikací manifestu.
  SPRING_DATASOURCE_PASSWORD: "change-me"

