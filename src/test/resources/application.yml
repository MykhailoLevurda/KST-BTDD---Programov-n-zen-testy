spring:
  datasource:
    url: jdbc:h2:mem:test;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      # V testech nespouštět data.sql – schéma vytvoří Hibernate, data vkládá @BeforeEach v IT.
      mode: never
